name: "Trigger Jobs"

# ============================================================================
# Manual trigger for remote jobs on a single selected target.
#
# Scope of this workflow:
#   - Data collection jobs on the selected server
#   - PostgreSQL backup on the selected server
#   - Database healthcheck on the selected server
#
# Cross-environment database copy is intentionally handled in a separate
# workflow so production operations and home-server restore stay explicit.
# ============================================================================

on:
  workflow_dispatch:
    inputs:
      target:
        description: "Target server"
        required: true
        type: choice
        options:
          - production
          - dev-hetzner
          - home-server
        default: production
      job:
        description: "Job to trigger on that server"
        required: true
        type: choice
        options:
          - saturday_night
          - option_data
          - market_start_mid_end
          - stock_data_daily
          - historization
          - db_backup
          - db_healthcheck
      dry_run:
        description: "Dry run (show command only)"
        type: boolean
        default: false

jobs:
  trigger:
    name: "${{ inputs.target }}: ${{ inputs.job }}"
    runs-on: ${{ inputs.target == 'home-server' && 'self-hosted' || 'ubuntu-latest' }}
    environment: ${{ inputs.target }}
    timeout-minutes: 360
    env:
      TARGET_LABEL: ${{ inputs.target == 'production' && 'Production' || inputs.target == 'home-server' && 'Home Server' || 'Dev Hetzner' }}

    steps:
      - name: Initialize
        id: init
        run: |
          echo "## Trigger Job - ${{ env.TARGET_LABEL }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Setting | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|---------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Target | ${{ inputs.target }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Job | ${{ inputs.job }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Dry Run | ${{ inputs.dry_run }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Triggered by | ${{ github.actor }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Started at | $(date -u '+%Y-%m-%d %H:%M:%S UTC') |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "start_time=$(date +%s)" >> $GITHUB_OUTPUT

      - name: Notify start via Telegram
        if: ${{ inputs.dry_run == false }}
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          TEXT="⚡ <b>[${{ env.TARGET_LABEL }}] Manual Job gestartet</b>%0A%0A👤 <b>${{ github.actor }}</b>%0A🎯 <code>${{ inputs.job }}</code>"
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d chat_id="${TELEGRAM_CHAT_ID}" \
            -d text="${TEXT}" \
            -d parse_mode=HTML >/dev/null 2>&1 || true

      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          printf '%s\n' "${{ secrets.DEPLOY_SSH_KEY }}" | tr -d '\r' > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key

      - name: Prepare remote command
        id: cmd
        run: |
          TARGET="${{ inputs.target }}"
          JOB="${{ inputs.job }}"

          if [ "$TARGET" = "home-server" ]; then
            DB_CONTAINER="skuld_staging_db"
            COMPOSE_CMD="docker compose -f /opt/skuld/docker-compose.yml -f /opt/skuld/docker-compose.testing.yml"
          else
            DB_CONTAINER="postgres_setup-db-1"
            COMPOSE_CMD="docker compose -f /opt/skuld/docker-compose.yml"
          fi

          if [ "$JOB" = "db_backup" ]; then
            DESCRIPTION="Database backup"
            cat > /tmp/remote_cmd.sh <<EOF
set -euo pipefail
TIMESTAMP=\$(date -u '+%Y%m%d_%H%M%S')
BACKUP_DIR=\$HOME/backups/postgres
BACKUP_FILE="\${BACKUP_DIR}/skuld_manual_\${TIMESTAMP}.sql.gz"
mkdir -p "\${BACKUP_DIR}"
echo "Starting pg_dump from ${DB_CONTAINER}..."
docker exec ${DB_CONTAINER} pg_dump -U admin -d Skuld | gzip > "\${BACKUP_FILE}"
FILESIZE=\$(du -h "\${BACKUP_FILE}" | cut -f1)
echo "Backup complete: \${BACKUP_FILE} (\${FILESIZE})"
ls -t "\${BACKUP_DIR}"/skuld_manual_*.sql.gz 2>/dev/null | tail -n +4 | xargs -r rm -f
EOF
          elif [ "$JOB" = "db_healthcheck" ]; then
            DESCRIPTION="Database healthcheck"
            cat > /tmp/remote_cmd.sh <<EOF
set -euo pipefail
echo "=== Docker containers ==="
${COMPOSE_CMD} ps || true
echo
echo "=== PostgreSQL ping ==="
docker exec ${DB_CONTAINER} psql -U admin -d Skuld -c "select now() as db_time, current_database() as db_name;"
echo
echo "=== Required view check ==="
docker exec ${DB_CONTAINER} psql -U admin -d Skuld -tAc "select 1 from pg_views where viewname = 'OptionDataMerged';"
echo
echo "=== View list filter ==="
docker exec ${DB_CONTAINER} psql -U admin -d Skuld -c "\\dv" | grep OptionDataMerged
EOF
          else
            DESCRIPTION="Data collection: ${JOB}"
            cat > /tmp/remote_cmd.sh <<EOF
set -euo pipefail
docker exec skuld-backend /bin/bash /app/Skuld/run_data_collection.sh ${JOB}
EOF
          fi

          chmod 700 /tmp/remote_cmd.sh
          echo "description=${DESCRIPTION}" >> $GITHUB_OUTPUT
          echo "### Command to execute" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo '```bash' >> $GITHUB_STEP_SUMMARY
          cat /tmp/remote_cmd.sh >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

      - name: Dry run
        if: ${{ inputs.dry_run == true }}
        run: |
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Dry run only. Nothing was executed on the target server." >> $GITHUB_STEP_SUMMARY

      - name: Execute on remote server
        if: ${{ inputs.dry_run == false }}
        id: execute
        run: |
          set +e
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no \
            -o ConnectTimeout=10 -o ServerAliveInterval=30 -o ServerAliveCountMax=5 \
            ${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }} 'bash -s' < /tmp/remote_cmd.sh > /tmp/remote_output.txt 2>&1
          EXIT_CODE=$?
          set -e

          echo "exit_code=${EXIT_CODE}" >> $GITHUB_OUTPUT

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Execution output" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo '<details><summary>Output (last 100 lines)</summary>' >> $GITHUB_STEP_SUMMARY
          echo '' >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          tail -100 /tmp/remote_output.txt >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo '</details>' >> $GITHUB_STEP_SUMMARY

          if [ "$EXIT_CODE" -ne 0 ]; then
            exit "$EXIT_CODE"
          fi

      - name: Calculate duration
        if: always()
        run: |
          START=${{ steps.init.outputs.start_time }}
          END=$(date +%s)
          DURATION=$((END - START))
          MINS=$((DURATION / 60))
          SECS=$((DURATION % 60))
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "Total duration: ${MINS}m ${SECS}s" >> $GITHUB_STEP_SUMMARY

      - name: Notify result via Telegram
        if: ${{ always() && inputs.dry_run == false }}
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          STATUS="${{ job.status }}"
          ICON="❌"
          if [ "$STATUS" = "success" ]; then
            ICON="✅"
          fi

          START=${{ steps.init.outputs.start_time }}
          END=$(date +%s)
          DURATION=$((END - START))
          MINS=$((DURATION / 60))
          SECS=$((DURATION % 60))

          TEXT="${ICON} <b>[${{ env.TARGET_LABEL }}] Manual Job ${STATUS}</b>%0A%0A👤 <b>${{ github.actor }}</b>%0A🎯 ${{ steps.cmd.outputs.description }}%0A⏱️ ${MINS}m ${SECS}s"
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d chat_id="${TELEGRAM_CHAT_ID}" \
            -d text="${TEXT}" \
            -d parse_mode=HTML >/dev/null 2>&1 || true